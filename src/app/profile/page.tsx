import React from "react";
import AccountView from "@/components/shared-components/auth/account-view";

export const metadata = { title: "Профиль | PINHEAD STUDIO" };

const ProfilePage = () => <AccountView />;

export default ProfilePage;
