import React from "react";
import OtpForm from "@/components/shared-components/auth/otp-form";

export const metadata = { title: "Подтверждение почты | PINHEAD STUDIO" };

const VerifyPage = () => <OtpForm />;

export default VerifyPage;
