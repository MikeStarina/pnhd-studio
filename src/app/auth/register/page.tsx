import React from "react";
import RegisterForm from "@/components/shared-components/auth/register-form";

export const metadata = { title: "Регистрация | PINHEAD STUDIO" };

const RegisterPage = () => <RegisterForm />;

export default RegisterPage;
