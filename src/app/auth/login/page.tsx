import React from "react";
import LoginForm from "@/components/shared-components/auth/login-form";

export const metadata = { title: "Вход | PINHEAD STUDIO" };

const LoginPage = () => <LoginForm />;

export default LoginPage;
